plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.dreamclippers.baseflutter"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17
    }

    defaultConfig {
        applicationId = "com.dreamclippers.baseflutter"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    // flavorDimensions "default"
    // productFlavors {
    //     develop {
    //         manifestPlaceholders = [applicationName: "Dev Base Flutter"]
    //         applicationIdSuffix ".dev"
    //         dimension "default"
    //     }
    //     staging {
    //         manifestPlaceholders = [applicationName: "STG Base Flutter"]
    //         // applicationIdSuffix ".stg"
    //         dimension "default"
    //     }
    //     production {
    //         manifestPlaceholders = [applicationName: "Base Flutter"]
    //         dimension "default"
    //     }
    // }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
